apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "yelb.fullname" . }}-redis
  labels:
    app: redis
spec:
  serviceName: {{ include "yelb.fullname" . }}-redis-headless
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          command:
            - sh
            - -c
            - |
              redis-server /data/redis.conf --protected-mode no --port 6379 --requirepass $REDIS_PASSWORD --masterauth $REDIS_PASSWORD
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-global-secrets
                  key: redis_password
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: redis-data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
